apiVersion: apps/v1
kind: Deployment
metadata:
  name: platforms-deployment
spec:
  selector:
    matchLabels:
      app: platformservice
  replicas: 1
  template:
    metadata:
      labels:
        app: platformservice
    spec:
      containers:
        - name: platformservice
          image: itsigald/platformservice:latest